version: '3'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    restart: on-failure
    command: ["/bin/sh", "-c", "./bin/run_app.sh"]
    volumes:
      - app_gomod_data:/go/pkg/mod
      - .:/src
    env_file:
      - app.env

volumes:
  app_gomod_data: